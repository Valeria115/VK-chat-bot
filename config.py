# import os
# from dotenv import load_dotenv

# load_dotenv(override=True)


# CONFIRMATION_TOKEN = str(os.getenv("CONFIRMATION_TOKEN"))

# ACCESS_TOKEN = str(os.getenv("ACCESS_TOKEN"))

# WEBHOOK_URL = str(os.getenv("WEBHOOK_URL"))
